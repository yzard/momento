package io.github.yzard.momento.app.navigation

enum class Destination(val label: String) {
    PHOTOS("Photos"), COLLECTIONS("Collections"), CREATE("Create"), SEARCH("Search"), SETTINGS("Profile & settings"), ALBUMS("Albums"), MAP("Map"), PLACES("Places"), FACES("Faces"), DEDUPLICATE("Deduplicate"), TRASH("Trash"), ADMIN("Admin"), VIEWER("Viewer")
}
